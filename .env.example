# Video Studio — environment secrets (template).
# Copy this file to .env and paste your key(s). The .env file is gitignored —
# never commit it. Re-running gemini-video/scripts/setup.sh copies this file
# to .env automatically if .env does not exist yet.

# Gemini multimodal API key (paid, opt-in). Used by the gemini-video skill to
# WATCH reference videos. Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=
